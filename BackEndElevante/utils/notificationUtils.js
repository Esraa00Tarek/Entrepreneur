// Temporary dummy notification utility for development (ES Modules)

export function notifyUser(userId, message) {
  // This is a placeholder. No real notification is sent.
  console.log(`[Dummy Notification] To user: ${userId}, message: ${message}`);
} 